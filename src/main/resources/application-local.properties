spring.application.name=CRUD-Template-Project
server.port=9000

# MySQL Database Configuration
spring.datasource.name=kvrmdb
spring.datasource.url=jdbc:mysql://localhost:3306/kvrmdb
# spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
# spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.datasource.username=kvrmmysql
spring.datasource.password=Mysql#123
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# Hibernate Configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

rsa.private-key=classpath:certs/private.pem
rsa.public-key=classpath:certs/public.pem

#management.endpoints.web.exposure.include=metrics,loggers,prometheus,health,info
management.endpoints.web.exposure.include=*
management.endpoint.loggers.enabled=true
management.metrics.export.prometheus.enabled=true
management.endpoints.enabled-by-default=true
management.endpoint.health.show-details=always
# http://localhost:9000/actuator/loggers

# Security configuration
spring.security.user.name=KVRM
spring.security.user.password=password

# Eureka discovery server details:
eureka.client.serviceUrl.defaultZone= http://localhost:8761/eureka/
# eureka.client.serviceUrl= http://${eureka.instance.hostname}:${server.port}/eureka

eureka.client.register-with-eureka= true
eureka.client.fetch-registry= true
eureka.client.serviceUrl= http://localhost:8761/eureka/
#eureka.client.serviceUrl.defaultZone= ${EUREKA_SERVER_ADDRESS:http://localhost:8761/eureka}

#loggin.level.com.productapi=TRACE
##loggin.level.root=DEBUG

# Date - Log Level - class of the log - thread executing - Message - newline
#logging.pattern.console=%d [%level] %c{1.} [%t} %m%n
#logging.file=CRUD-Template-log.log
#logging.pattern.file=%d [%level] %c{1.} [%t} %m%n

spring.boot.admin.client.url=http://localhost:8090

welcome.message="Hi Murthy"
application.hello="Hello Murthy"
#spring.devtools.add-properties=false

# Tailwinds CSS properties to follow:
spring.mvc.static-path-pattern=/**
